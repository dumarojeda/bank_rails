%h1 Clientes

%table.table.table-striped
  %thead
    %tr
      %th ID
      %th Nombre
      %th Nº Cedula
      %th Celular
      %th Ciudad
      %th Email
      %th Cuentas
      %th Editar
      %th Borrar
  %tbody
  - @customers.each do |customer|
    %tr
      %td
        = customer.id
      %td
        = customer.first_name + customer.last_name
      %td
        = customer.document_number
      %td
        = customer.phone
      %td
        = customer.city
      %td
        = customer.email
      %td
        = customer.accounts.count
      %td
        = link_to ' Editar', edit_customer_path(customer)
      %td
        = link_to ' Eliminar', customer_path(customer), method: :delete, data: { confirm: '¿Estas seguro?' }

= link_to "Crear cliente", new_customer_path
