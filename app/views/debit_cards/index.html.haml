%h1 Tarjetas debito

%table.table.table-striped
  %thead
    %tr
      %th ID
      %th Numero de cuenta
      %th Cliente
      %th Editar
      %th Borrar
  %tbody
  - @debit_cards.each do |card|
    %tr
      %td
        = card.id
      %td
        = card.account_id
      %td
        = card.customer_id
      %td
        = #link_to ' Editar', edit_debit_card_path(account)
      %td
        = #link_to ' Eliminar', debit_cards_path(account), method: :delete, data: { confirm: '¿Estas seguro?' }

= link_to "Crear tarjeta", new_debit_card_path
